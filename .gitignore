# ---------- 依赖 ----------
**/node_modules/
**/package-lock.json.bak
# monorepo 顶层 lock 必须跟踪;子包旧 lock 由 npm workspaces 协调
packages/**/package-lock.json
dsh-plugin/**/package-lock.json

# ---------- 构建产物 ----------
**/dist/
**/build/
**/.next/
**/.nuxt/
**/.output/
**/.vite/

# ---------- 环境变量 / 密钥 ----------
.env
.env.local
.env.*.local
.env.production
.env.development
**/.env
!**/.env.example

# ---------- 运行时 / 数据库 ----------
**/data/*.db
**/data/*.db-journal
**/data/*.db-shm
**/data/*.db-wal
data/build.log
data/logs/
data/*.log
**/*.sqlite
**/*.sqlite-journal

# ---------- IDE / 编辑器 ----------
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ---------- 系统文件 ----------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# ---------- 日志 ----------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# ---------- 临时文件 ----------
*.tmp
*.temp
.cache/
.tmp/
.temp/

# ---------- 测试 / 覆盖率 ----------
coverage/
.nyc_output/
*.lcov

# ---------- TypeScript ----------
*.tsbuildinfo

# ---------- Docker ----------
.docker-data/

# ---------- 工作流数据 ----------
workflow/data/
workflow/.n8n/

# ---------- 爬虫产物 ----------
crawler/output/
crawler/storage/
crawler/screenshots/

# ---------- 项目级特殊文件 ----------
workflow/

# 桌面版安装包与发布产物(走 GitHub Releases,不进 git 树)
RELEASE/

# ---------- monorepo 顶层 ----------
# packages/* 子目录的构建产物已由 **/dist/ 覆盖,这里仅补充
packages/*/coverage/
packages/*/.tsbuildinfo
packages/*/src/**/*.js.map
# monorepo 顶层 build.log 排除
/build.log
/build-*.log
# 桌面版打包缓存与临时脚本
patch_bs.cjs
desktop/dist/
desktop/win-unpacked/
desktop/verify-out.txt
# NSIS 编译调试日志 (主走马灯调试的临时输出)
desktop/makensis-stdout.txt
desktop/makensis-stderr.txt
# 构建调试期生成的临时脚本与文档
desktop/clean-temp-dists.ps1
desktop/release-readme.txt

# ---------- 历史归档(不入库) ----------
历史文档/**/*.zip

# ---------- 调试期临时截图 ----------
diag-*.png
diagnostic-result.png